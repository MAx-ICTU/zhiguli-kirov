# Backend и админка

Публичный каталог остается статическим и продолжает работать на GitHub Pages. Backend запускается отдельно для рабочего кабинета магазина: редактирование товаров, прием заявок и будущая синхронизация с учетной системой.

## Запуск

```bash
npm run admin
```

Админка и API будут доступны по адресу:

```text
http://localhost:4173/admin.html
```

Для закрытого режима задайте токен:

```bash
ADMIN_TOKEN=change-me npm run admin
```

После этого запросы на изменение каталога и просмотр заявок должны передавать заголовок:

```text
Authorization: Bearer change-me
```

Если админка открыта в браузере, она запросит токен при первом защищенном действии и сохранит его в этом браузере.

## Что уже есть

- `GET /api/health` - проверка работы backend.
- `GET /api/catalog/summary` - сводка по каталогу и разделам.
- `GET /api/products` - список товаров с поиском, разделом, статусом и пагинацией.
- `GET /api/products/:code` - карточка товара.
- `PATCH /api/products/:code` - сохранение ручных правок товара.
- `POST /api/requests` - прием запроса покупателя.
- `GET /api/requests` - список входящих запросов для менеджера.
- `PATCH /api/requests/:id` - смена статуса входящего запроса.
- `POST /api/imports/preview` - предварительная проверка нового прайса.
- `POST /api/imports/:id/apply` - публикация проверенного импорта.
- `GET /api/imports/history` - история публикаций каталога.
- `GET /api/sync/status` - статус готовности к будущей синхронизации.

## Заявки

В админке есть доска заявок с тремя состояниями: новые, в работе и закрытые. Менеджер видит телефон, комментарий покупателя и товары из списка запроса, после чего может перевести обращение в работу или закрыть его.

Уведомления на внешние контакты пока не включены. Новые обращения сохраняются только во внутреннем хранилище backend и видны в админке.

## Импорт прайса

Импорт работает в два шага: сначала админка показывает предварительную проверку, затем менеджер отдельно публикует каталог. Preview считает новые, измененные и исчезнувшие позиции, а также показывает примеры строк.

Сейчас интерфейс принимает JSON, CSV, TSV и HTML-таблицы, которые иногда сохраняются с расширением `.xls`. Для бинарного Excel-файла следующим шагом нужен отдельный серверный парсер или конвертация в CSV перед загрузкой.

## Хранение данных

Исходный каталог остается в `src/products.js`, чтобы не ломать опубликованный сайт. Ручные правки админки сохраняются в `server/data/catalog-overrides.json`, запросы покупателей - в `server/data/requests.json`, история публикаций - в `server/data/import-history.json`.

Такой слой можно заменить на SQLite/PostgreSQL без изменения публичного сценария: коды товаров из прайса сохраняются как стабильные внешние идентификаторы для будущей синхронизации.
